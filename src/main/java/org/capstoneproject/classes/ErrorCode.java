/**
 *	A class used to serve as an enumerated type in order to avoid using constants.
 *	The different possible status of a request are represented here.
*/
package org.capstoneproject.classes;

public enum ErrorCode {REQUEST_OK, REQUEST_BAD, REQUEST_FORBIDDEN, REQUEST_NOT_FOUND}
